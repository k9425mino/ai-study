# Codex 슬래시 명령어 치트시트

> **정리 기준:** 2026-07-26
>
> **대상:** Codex CLI · Codex IDE 확장
>
> 자주 쓰는 명령만 추린 실전용 요약입니다. Codex에서 `/`를 입력하면 현재 환경에서 사용할 수 있는 전체 명령을 확인할 수 있습니다.

## ⭐ 이것부터 기억하기

| 명령어 | 한 줄 설명 | 추천 상황 |
| --- | --- | --- |
| `/model` | 모델을 변경합니다. | 작업 난이도에 맞춰 모델을 바꿀 때 |
| `/permissions` | Codex의 파일·명령 실행 권한을 조절합니다. | 읽기 전용과 자동 실행 범위를 바꿀 때 |
| `/plan` | 구현 전에 계획부터 세우는 모드로 전환합니다. | 요구사항이 복잡하거나 변경 범위가 클 때 |
| `/mention` | 파일이나 폴더를 대화에 첨부합니다. | 분석할 대상을 정확히 지정할 때 |
| `/diff` | Git 변경사항과 미추적 파일을 보여줍니다. | 커밋 전에 수정 내용을 빠르게 확인할 때 |
| `/review` | 현재 변경사항에 대한 코드 리뷰를 시작합니다. | 버그·보안·테스트 누락을 점검할 때 |
| `/status` | 모델, 권한, 작업 경로, 컨텍스트 사용량을 확인합니다. | 현재 세션 설정이 헷갈릴 때 |
| `/compact` | 긴 대화를 요약해 컨텍스트 공간을 확보합니다. | 대화가 길어져 응답 품질이 떨어질 때 |
| `/new` | 같은 프로젝트에서 새 대화를 시작합니다. | 이전 작업과 무관한 일을 시작할 때 |
| `/resume` | 저장된 이전 대화를 이어서 엽니다. | 중단했던 작업을 계속할 때 |

> `/permissions`, `/mention`, `/diff`, `/new`, `/resume`은 **CLI 중심 명령**입니다. IDE에서는 일부 기능을 버튼이나 컨텍스트 선택기로 제공합니다.

## 🧭 상황별로 골라 쓰기

### 1. 작업을 시작할 때

| 명령어 | 용도 |
| --- | --- |
| `/init` | 현재 폴더에 기본 `AGENTS.md`를 생성합니다. 생성 후 프로젝트 규칙에 맞게 수정하세요. |
| `/model` | 현재 작업에 사용할 모델을 선택합니다. |
| `/reasoning` | 추론 강도를 선택합니다. **IDE에서 제공**됩니다. |
| `/personality` | 지원 모델에서 답변 스타일을 선택합니다. |
| `/permissions` | Codex가 묻지 않고 수행할 수 있는 범위를 설정합니다. **CLI에서 제공**됩니다. |
| `/status` | 선택한 모델과 권한이 제대로 적용됐는지 확인합니다. |

### 2. 큰 작업을 설계할 때

| 명령어 | 용도 |
| --- | --- |
| `/plan` | 코드 수정 전, 범위와 순서를 계획합니다. |
| `/goal` | 오래 걸리는 작업에서 지속적으로 추적할 목표를 설정·조회합니다. |
| `/mention` | 관련 파일이나 폴더를 명시적으로 첨부합니다. **CLI에서 제공**됩니다. |
| `/ide-context` | 열어 둔 파일과 선택 영역 등 IDE 컨텍스트 자동 포함을 켜거나 끕니다. **IDE에서 제공**됩니다. |

### 3. 수정 내용을 확인할 때

| 명령어 | 용도 |
| --- | --- |
| `/diff` | Codex가 수정한 Git diff를 확인합니다. **CLI에서 제공**됩니다. |
| `/review` | 미커밋 변경이나 기준 브랜치와의 차이를 리뷰합니다. |
| `/copy` | Codex의 마지막 완료 응답을 클립보드에 복사합니다. **CLI에서 제공**됩니다. |
| `/status` | 남은 컨텍스트와 현재 세션 설정을 다시 확인합니다. |

### 4. 대화가 길어졌을 때

| 명령어 | 용도 |
| --- | --- |
| `/compact` | 현재 대화의 핵심만 요약해 컨텍스트를 절약합니다. |
| `/side` 또는 `/btw` | 본 대화를 흐트러뜨리지 않고 짧은 임시 질문을 합니다. |
| `/fork` | 현재 대화를 복제해 다른 접근을 실험합니다. |
| `/new` | 완전히 새로운 대화를 시작합니다. **CLI에서 제공**됩니다. |
| `/resume` | 저장된 대화를 선택해 이어서 작업합니다. **CLI에서 제공**됩니다. |
| `/rename` | 나중에 찾기 쉽도록 현재 대화 이름을 바꿉니다. **CLI에서 제공**됩니다. |

### 5. 도구와 확장 기능을 확인할 때

| 명령어 | 용도 |
| --- | --- |
| `/skills` | 설치된 스킬을 찾아 현재 작업에 사용합니다. **CLI에서 제공**됩니다. |
| `/plugins` | 플러그인을 검색·설치·관리합니다. **CLI에서 제공**됩니다. |
| `/apps` | 연결된 앱을 찾아 프롬프트에 추가합니다. **CLI에서 제공**됩니다. |
| `/mcp` | 연결된 MCP 서버와 도구 상태를 확인합니다. |
| `/hooks` | 등록된 라이프사이클 훅을 확인하고 관리합니다. **CLI에서 제공**됩니다. |

### 6. 실행 상태와 사용량을 볼 때

| 명령어 | 용도 |
| --- | --- |
| `/ps` | 백그라운드 터미널과 최근 출력을 확인합니다. **CLI에서 제공**됩니다. |
| `/stop` | 현재 세션이 시작한 백그라운드 터미널을 중지합니다. **CLI에서 제공**됩니다. |
| `/usage` | 계정 토큰 사용량과 제한 초기화 시점을 확인합니다. **CLI에서 제공**됩니다. |
| `/fast` | 지원되는 모델에서 Fast 서비스 티어를 켜거나 끕니다. |

## 💻 IDE에서 특히 유용한 명령

| 명령어 | 용도 |
| --- | --- |
| `/local` | 현재 로컬 작업공간에서 대화를 실행합니다. |
| `/cloud` | 사용 가능한 경우 대화를 클라우드에서 실행합니다. |
| `/cloud-environment` | 대화에 사용할 클라우드 환경을 선택합니다. |
| `/worktree` | 새 Git worktree에서 대화를 실행합니다. |
| `/project` | 새 대화에 사용할 프로젝트를 선택합니다. |
| `/ide-context` | 열어 둔 파일과 선택 영역의 자동 전달 여부를 조절합니다. |

## 🚀 추천 사용 흐름

```text
/status
   ↓
/model 또는 /permissions
   ↓
/plan
   ↓
/mention
   ↓
작업 수행
   ↓
/diff
   ↓
/review
   ↓
/compact 또는 /new
```

IDE에서는 `/mention`과 `/diff` 대신 파일 컨텍스트 선택기와 변경사항 리뷰 화면을 활용하면 됩니다.

## 알아두면 좋은 점

- 명령 지원 여부는 **CLI·IDE 같은 사용 화면**, Codex 버전, 계정 권한, 활성화된 기능에 따라 달라질 수 있습니다.
- 가장 정확한 목록은 현재 사용 중인 Codex 입력창에서 `/`를 입력해 확인하세요.
- `/compact`는 대화를 삭제하는 명령이 아니라 핵심 내용을 요약해 컨텍스트를 줄이는 명령입니다.
- `/review`는 변경사항을 분석하며, 기본적으로 작업 트리를 직접 수정하지 않습니다.
- `/delete`는 현재 세션과 하위 세션을 영구 삭제할 수 있으므로 신중하게 사용하세요.
- `/quit`과 `/exit`는 같은 종료 명령입니다. 중요한 변경사항을 저장하거나 커밋한 뒤 사용하세요.

## 공식 문서

- [Codex CLI 슬래시 명령어](https://learn.chatgpt.com/docs/developer-commands?surface=cli)
- [Codex IDE 확장 슬래시 명령어](https://learn.chatgpt.com/docs/developer-commands?surface=ide)
